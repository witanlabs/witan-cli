{
  "name": "@witan/rpc-typegen",
  "version": "0.10.0",
  "private": true,
  "description": "JSON Schema → TypeScript / Python type generator for Witan RPC",
  "type": "module",
  "scripts": {
    "build-schema": "node src/build-schema.js",
    "generate": "node src/generate.js",
    "build": "npm run build-schema && npm run generate"
  },
  "dependencies": {
    "json-schema-to-typescript": "^15.0.0"
  }
}
